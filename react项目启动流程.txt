1.确保数据库服务启动，可以使用Studio 3T测试。
2.删除其他无关数据库，local库可以保留（这一步仅第一次执行，以后不用）
3.启动后台服务器---若成功，则显示服务器地址为localhost：4000，不要在浏览器中打开！！！
4.使用Studio 3T向admin_db中导入数据（这一步仅第一次执行，以后不用）
5.启动前台应用 --- 若成功，浏览器会自动打开前台应用localhost:3000/login

备注：
	1.项目路径尽量不要出现中文
	2.最好将MongoDB服务设为开机自启动